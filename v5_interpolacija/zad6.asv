% Lagranžovom interpolacijom odrediti parametre a 0 , a 1 , a 2 i a 3 tako da funkcija f(x) = a 0 + a 1 x + a 2 x 2 + a 3 x 3
zadovoljava slede?e:
x = [0.1, 0.3, 0.6, 1.2];
fX = [1.023, 1.261, 2.368, 9.064];

p = lagrangeInterp(x, fX);

disp(['a0', num2str(p(4))]);
disp(['a1', num2str(p(3))]);
disp(['a2', num2str(p(2))]);
disp(['a3', num2str(p(1))]);